import 'const.dart';

const regular = "popins";
const semibold = "popins";
const bold = "popins";

const appBarTitleLight =
    TextStyle(fontFamily: semibold, fontSize: 18, color: Colors.black);
const appBarTitle =
    TextStyle(fontFamily: semibold, fontSize: 18, color: Colors.black);

const cityTitle = TextStyle(
    fontFamily: bold,
    fontSize: 30,
    color: Colors.black,
    fontWeight: FontWeight.w500);

const blackText =
    TextStyle(fontFamily: semibold, fontSize: 17, color: Colors.black);
const whiteText =
    TextStyle(fontFamily: semibold, fontSize: 17, color: Colors.white);
